apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: tumor-classifier
spec:
  # ClusterIP: internal only — the frontend Nginx proxies /api to this service.
  type: ClusterIP
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
