apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: tumor-classifier
spec:
  # LoadBalancer: GKE provisions an external IP automatically.
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
