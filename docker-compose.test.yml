services:
  backend-test:
    build:
      context: ./backend
      dockerfile: Dockerfile.test
    environment:
      TF_ENABLE_ONEDNN_OPTS: "0"
    # Exit code is propagated so CI can detect failures.
    # Run: docker compose -f docker-compose.test.yml up --exit-code-from backend-test
